{
  "name": "amethyst-functions",
  "description": "Amethyst callable functions for Super Admin user management",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "seed-profiles": "node scripts/seed_user_profiles.js",
    "reset-data": "node scripts/reset_operational_data.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2"
  },
  "private": true
}
