-- =============================================================================
-- Amethyst — مسح بيانات التشغيل مع الإبقاء على المستخدمين فقط
-- PostgreSQL (مطابق لـ prisma/schema.prisma)
--
-- ماذا يفعل:
--   - يحذف جميع الصفوف من جداول العمليات (مركبات، منتجات، تحميلات، مبيعات، مصاريف، سجلات تدقيق، …).
--   - لا يمس جدول users (لا حذف مستخدمين ولا أدوارهم المخزنة في نفس الجدول).
--   - لا يمس _prisma_migrations (سجل الهجرات).
--   - RESTART IDENTITY: إعادة تعيين أي أعمدة SERIAL/IDENTITY إن وُجدت لاحقاً.
--   - CASCADE: يتعامل مع قيود المفاتيح الأجنبية بين الجداول المُفرّغة فقط.
--
-- تحذير: عملية مدمرة للبيانات التشغيلية. نفّذ على نسخة احتياطية أو بيئة تطوير.
-- =============================================================================

BEGIN;

TRUNCATE TABLE
  "audit_logs",
  "expenses",
  "vehicle_sales",
  "station_debt_entries",
  "station_sales",
  "vehicle_loads",
  "vehicles",
  "products"
RESTART IDENTITY CASCADE;

COMMIT;
